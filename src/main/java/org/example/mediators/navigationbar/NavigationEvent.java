package org.example.mediators.navigationbar;

import org.example.mediators.internal.MediatorEvent;

public enum NavigationEvent implements MediatorEvent {
    GOTO_PURCHASE_LISTING,
    PURCHASE_SELECTED
}
